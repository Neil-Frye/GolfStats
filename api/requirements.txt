# Ultra-minimal requirements for Vercel serverless function
# ONLY include what's directly imported in api/index.py
Flask==2.3.2
python-dotenv==1.0.0
supabase==1.0.3

# Dependencies that are EXPLICITLY NOT INCLUDED:
# - selenium (heavy browser automation)
# - webdriver-manager (heavy browser automation)
# - pyppeteer (heavy browser automation)
# - psycopg2-binary (supabase handles db connections)
# - email-validator (not used in minimal API)
# - gunicorn (Vercel handles server)
# - cryptography (not directly imported)
# - requests (already included by supabase)